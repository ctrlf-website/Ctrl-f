export interface MiWeb {
  hero: {
    title: string;
    textColor: string;
    textFamily: string;
    backgroundColor: string;
  };
}

export interface HeroFormValues {
  title: string;
  textColor: string;
  textFamily: string;
  backgroundColor: string;
}
